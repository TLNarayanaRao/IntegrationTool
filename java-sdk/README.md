# Java activity SDK

Implement `FabricActivity`, package it as a Java 17 JAR, and configure a Java worker command on a Java activity. The runtime exchanges one JSON object per invocation, so the worker can be independently deployed or replaced.
