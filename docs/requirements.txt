- Nested canvas rendering and editing
- Group membership and configuration persistence
- Runtime execution graph scheduling
- Loop iteration and retry semantics
- JDBC transaction lifecycle
- Exception propagation
- Validation rules
- Deployment/archive schemas
- Debugger behavior
Implementing only the UI would create groups that appear configured but do not execute correctly. The implementation needs to continue as a staged runtime feature rather than being represented as complete prematurely.