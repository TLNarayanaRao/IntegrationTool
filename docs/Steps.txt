$ca = 'C:\Certificates\company-ca-chain.pem'

npm.cmd config set cafile "$ca" --location=user
npm.cmd config set strict-ssl true --location=user

py -3.11 -m pip config --user set global.cert "$ca"

[Environment]::SetEnvironmentVariable('NODE_EXTRA_CA_CERTS', $ca, 'User')
[Environment]::SetEnvironmentVariable('PIP_CERT', $ca, 'User')

$env:NODE_EXTRA_CA_CERTS = $ca
$env:PIP_CERT = $ca
$env:REQUESTS_CA_BUNDLE = $ca

cd C:\Narayana\Integration-Tool\Software\IntegrationFabric

Get-Process node,electron,IntegrationFabricRuntime -ErrorAction SilentlyContinue |
    Stop-Process -Force

Remove-Item -LiteralPath '.\frontend\node_modules' -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -LiteralPath '.\backend\.venv' -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -LiteralPath '.\backend\build' -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -LiteralPath '.\backend\dist' -Recurse -Force -ErrorAction SilentlyContinue

npm.cmd cache verify

cd frontend
npm.cmd ci
npm.cmd run desktop:installer
