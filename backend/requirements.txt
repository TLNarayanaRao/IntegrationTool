fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic>=2.12,<3
httpx==0.28.1
python-multipart==0.0.20
aiofiles==24.1.0
paramiko==3.5.1
# External connector adapters (the local in-memory design-time broker also remains available):
stomp.py>=8.2,<9                    # JMS providers exposing STOMP, including TIBCO EMS
confluent-kafka>=2.6,<3            # Apache Kafka producer, consumer, security, and transactions
fastavro>=1.9,<2                   # Kafka Avro/schema-registry payload support
google-cloud-pubsub>=2.25,<3
snowflake-connector-python>=3.16,<4  # Snowflake JDBC-equivalent external runtime
cryptography>=44                    # Snowflake key-pair authentication
pika>=1.3.2                         # RabbitMQ AMQP 0.9.1
# Apache Qpid's 0.40 source distribution has no CPython 3.11 Windows wheel and
# therefore requires Visual C++ during desktop builds. This distribution ships
# the same `proton` modules as a reproducible CPython 3.11 wheel.
python-qpid-proton-wheel==0.39.0   # AMQP 1.0 brokers
azure-servicebus>=7.14              # Azure Service Bus AMQP runtime
azure-identity>=1.20                # Azure OAuth and managed identity
openpyxl>=3.1.5                     # Excel workbook reader
xlrd>=2.0.1                         # Legacy .xls workbook reader
psycopg[binary]>=3.2                # PostgreSQL JDBC-equivalent runtime
PyMySQL>=1.1                        # MySQL and MariaDB
oracledb>=2.5                       # Oracle Database
pyodbc>=5.2                         # Microsoft SQL Server and ODBC
# pyrfc>=3.3          # also requires the separately licensed SAP NetWeaver RFC SDK
