fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic>=2.12,<3
httpx==0.28.1
python-multipart==0.0.20
aiofiles==24.1.0
paramiko==3.5.1
# External connector adapters (the local in-memory design-time broker works without these):
# stomp.py>=8.2       # TIBCO EMS with the EMS STOMP service enabled
# confluent-kafka>=2.6
# google-cloud-pubsub>=2.25
snowflake-connector-python>=3.16,<4  # Snowflake JDBC-equivalent external runtime
cryptography>=44                    # Snowflake key-pair authentication
# pyrfc>=3.3          # also requires the separately licensed SAP NetWeaver RFC SDK
